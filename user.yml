---
- hosts: localhost 
  become: true
  tasks:
    - name: user creation
      user:
        name: prasanta
        state: present

    - name: docker install
      yum:
        name: docker
        state: present

    - name: docker up
      service:
        name: docker
        state: started
    - name: docker command execute
      command: docker build -t containern /Jenkins_test/dockerfile

    - name: port
      command: docker run -d -p 8080:80 --name nginxma containern
    

    
     

